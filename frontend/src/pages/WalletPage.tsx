import { useState } from 'react'
import { ArrowDownLeft, ArrowUpRight, CreditCard, Plus, WalletCards, Zap } from 'lucide-react'

export function WalletPage() { const [tab, setTab] = useState('overview'); const [added, setAdded] = useState(false); return <div className="page-stack"><div className="page-title-row"><div><span className="eyebrow">Votre espace financier</span><h1>Portefeuille <em>du club.</em></h1><p>Gérez vos jetons et suivez vos dernières activités.</p></div><button className="button button-gold" onClick={() => setAdded(true)}><Plus size={17}/> Ajouter des jetons</button></div><div className="wallet-layout"><section><div className="balance-card"><div className="balance-top"><span>Solde disponible</span><WalletCards size={18}/></div><strong>{added ? '22 450' : '12 450'} <small>jetons</small></strong><div className="balance-footer"><span>≈ {added ? '22 450' : '12 450'} Ar en mode simulation</span><span className="balance-up"><ArrowUpRight size={13}/> +12,8%</span></div></div><div className="wallet-tabs">{['overview', 'history'].map((item) => <button className={tab === item ? 'active' : ''} onClick={() => setTab(item)} key={item}>{item === 'overview' ? 'Vue d’ensemble' : 'Historique'}</button>)}</div>{tab === 'overview' ? <><div className="section-heading compact"><div><span className="eyebrow">Rapide & sécurisé</span><h2>Recharger en 2 clics</h2></div></div><div className="deposit-grid"><button onClick={() => setAdded(true)}><span>+ 5 000</span><small>jetons</small></button><button className="featured" onClick={() => setAdded(true)}><span>+ 10 000</span><small>jetons <b>Populaire</b></small></button><button onClick={() => setAdded(true)}><span>+ 25 000</span><small>jetons</small></button></div></> : <div className="activity-card"><ActivityRow icon={<ArrowDownLeft/>} title="Bonus de bienvenue" date="Aujourd’hui, 10:42" amount="+ 10 000" positive/><ActivityRow icon={<ArrowUpRight/>} title="Table Émeraude" date="Aujourd’hui, 09:18" amount="− 2 500"/><ActivityRow icon={<ArrowDownLeft/>} title="Recharge simulation" date="Hier, 19:04" amount="+ 5 000" positive/></div>}</section><aside className="wallet-aside"><div className="payment-card"><span className="eyebrow gold"><Zap size={13}/> Bientôt disponible</span><h3>Jouez sans limites.</h3><p>Les paiements mobile money arrivent bientôt. Pour l’instant, profitez du mode simulation sans risque.</p><div className="payment-methods"><span>MVola</span><span>Orange Money</span><span>Airtel Money</span></div><button className="button button-outline full"><CreditCard size={16}/> Me prévenir</button></div><div className="secure-note"><WalletCards size={18}/><div><strong>Solde protégé</strong><span>Votre historique reste privé et accessible à tout moment.</span></div></div></aside></div></div> }
function ActivityRow({ icon, title, date, amount, positive }: {icon: React.ReactNode; title: string; date: string; amount: string; positive?: boolean}) { return <div className="activity-row"><span className={`activity-icon ${positive ? 'positive' : ''}`}>{icon}</span><div><strong>{title}</strong><span>{date}</span></div><b className={positive ? 'positive-text' : ''}>{amount}</b></div> }
